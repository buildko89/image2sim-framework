# 顔の数値調整ガイド（koha9face）

作業モデル `output_v2/base/p3_koha9face.glb` の顔まわりを、自分で数値調整するためのガイド。
2026-07-15（P4-顔 第2段階その2）時点の値を「現在値」として記載。

調整は大きく2系統に分かれ、**再生成の手間が違う**:

| 系統 | 対象 | 変更後にやること | 所要 |
|---|---|---|---|
| A. テクスチャ・髭 | 目・鼻・虹彩・顔の柄・髭 | `python pipeline_v2/p3_run_koha9face.py` の1コマンドだけ | 数分 |
| B. メッシュ | 頬・耳の形・マズル・眉庇 | 下の「B の手順」全部（アンカー再マップが必要） | 10〜15分 |

どちらも実行の最後にゲート2本（qa_skin_stretch / qa_belly_bind）と 1440px キャプチャが自動で走る。
**OVERALL: PASS が2つ出ることを必ず確認**。結果画像は以下で見る:

- 顔のアップ4方向: `output_v2/reports/p1_shots/p3k9f_head_{front,left,right,top}.png`
- 全アニメ概観: `output_v2/reports/p1_contact_p3k9f_overview.png`

---

## A. テクスチャ・髭の調整（1コマンドで反映）

変更後: `python pipeline_v2/p3_run_koha9face.py`

### 目 — `pipeline_v2/p3_paint_texture_koha9face.py`

| 定数 | 現在値 | 意味・効く方向 |
|---|---|---|
| `EYE_ART_MAJOR` | 1.06 | 目の絵の横方向の拡大率。大きく=1.10〜1.15、小さく=0.95 |
| `EYE_ART_MINOR` | 0.84 | 目の絵の縦方向の拡大率。上げると丸目、下げると細目 |
| `EYE_TILT` | 0.18 | 釣り目の傾き（ラジアン。0.18≈10°）。0で水平、0.26≈15°でかなりきつい釣り目。符号は自動で「外眼角が上がる」向き |
| `EYES` | 下記 | 目の「開き」（見える範囲の楕円）。**絵ではなく開きの大きさ** |
| `EYE_LINER_R` | (0.86, 1.10) | アイラインの太さ（楕円半径に対する内/外）。外側を上げると太く |
| `IRIS_CELADON` | (175,190,152) | 虹彩の目標色 RGB（青磁グリーン） |
| `IRIS_MIX` | 0.55 | 虹彩の置換率。上げるほど均一なグリーン、下げるほど素材の色が残る |

```python
# EYES = [(中心 x,y,z), (半径 rx,ry,rz)] × 左右。単位 m、glTF系（x=左右, y=上下, z=前後）
EYES = [((-0.0212, 0.2092, 0.1902), (0.0103, 0.0072, 0.0092)),   # -X（猫の右、正面レンダーの画面左）
        ((+0.0217, 0.2098, 0.1886), (0.0099, 0.0062, 0.0090))]   # +X（猫の左、画面右）
```

- **目をもっと開く**: `ry`（2番目の半径）を両目 +0.001 ずつ。現在 0.0072/0.0062
- **目を横に長く**: `rx` を +0.0005 ずつ
- **注意**: +X の目は素材の絵が大きいので、左右で同じ値にせず**今の左右差を保ったまま**増減する
  （同じにすると +X だけ丸目になる。実測済み）
- 中心座標はメッシュ表面に合わせてあるので、**メッシュを変えない限り動かさない**

### 鼻 — 同ファイル

| 定数 | 現在値 | 意味 |
|---|---|---|
| `NOSE_ART_SCALE` | 1.3 | 鼻のピンクの絵の等方拡大率。1.0 で素材のまま |
| `NOSE` | ((0, 0.196, 0.2227), (0.011, 0.010, 0.008)) | 拡大を適用する範囲の楕円。拡大率を大きく上げるときは半径も少し広げる |

### 顔の柄 — 同ファイル `FACE_BLOBS`

各行 = `(名前, 中心(x,y,z), 半径(rx,ry,rz), 色, ゲート)`。色を変えるか、半径で斑を大小できる。
中心座標はメッシュ表面に合わせて再マップ済みなので、**位置の微調整は ±0.002 程度に留める**のが安全。

### 髭 — `pipeline_v2/p3_run_koha9face.py`（上のほう）

| 定数 | 現在値 | 意味 |
|---|---|---|
| `WHISKER_RADIUS` | "0.0004" | 髭の根元半径 (m)。細く=0.0003、太く=0.0005（第1弾は 0.00055 だった） |
| `WHISKER_COLOR` | "0.95,0.94,0.92" | 髭の色（0〜1 の RGB。ほぼ白） |
| `BROW_SCALE` | "0.7" | 眉の上の髭の長さ倍率。0 に近づけるほど控えめ |

髭の本数・長さ・垂れ方は `pipeline_v2/p3c_add_whiskers_blender.py` の `ROWS`
（各段 = 根元の高さ/横/前, 扇形の (方位角, 長さ) リスト, 立ち上がり, 垂れ）。
**長さは fan の 0.056 などの数値 (m)**。垂れ強く=最後の droop を増やす。
根元座標（0.2243 など）はメッシュ表面に合わせてあるので**動かさない**。

---

## B. メッシュの調整 — `pipeline_v2/p1a_rebind_blender.py`

### パラメータ

| 定数 | 現在値 | 意味・効く方向 |
|---|---|---|
| `MUZZLE_EXTEND` | 0.025 | 鼻先を前へ出す量 (m)。第1段階 0.017 → 現在 0.025 |
| `MUZZLE_NARROW` | 0.05 | マズル先端を細める率。上げると尖る |
| `HEAD_WIDTH_PROFILE` | 頬ピーク 1.22 | 高さごとの横幅倍率の折れ線。`(0.206, 1.22)` が頬骨のピーク。頬をもっと張る=1.25〜1.30 |
| `EAR_TALL` | 1.30 | 耳の高さ倍率（第1段階 1.55 → 低くした） |
| `EAR_WIDEN` | 1.30 | 耳の板の横幅倍率（耳ごとの中心まわり） |
| `EAR_SPREAD_BASE` | 0.009 | 耳の付け根を外へ (m) |
| `EAR_SPREAD_TIP` | 0.020 | 耳の先端を外へ (m)。BASE との差が「外への倒れ」になる |
| `EAR_WELD_FROM` | 0.70 | 先端収束（V割れ対策）を始める高さの割合。下げると尖りが長く |
| `EAR_TIP_WELD` | 0.85 | 先端での収束率。1.0 で完全に1点。V が見えたら上げる |
| `BROW_OUT` | 0.0055 | 眉庇（おでこ）を前へ出す量 (m)。彫りを深く=0.007、控えめ=0.004 |
| `BROW_H` | (0.212, 0.248) | 眉庇の高さ帯。**下端 0.212 は目の高さ 0.209 のすぐ上なので下げない** |
| `HEAD_SCALE` | 1.04 | 頭全体の拡大率（首の付け根支点） |

**マズルの見た目の長さは、上の `MUZZLE_EXTEND`（B: 上流の重い手順）ではなく
`p3_run_koha9face.py` の `MUZZLE_PULL`（現在 "0.33"）で調整するのが速い。**
これは face ランナー内の後段ツール `p5_muzzle_shorten_blender.py`（4.962）が使う値で、
目の面(Y=-0.185)〜鼻先を一様圧縮する（0.33 = 1/3 短縮 = 鼻先 14.4mm 後退。0 で無効）。
頂点のみ移動なので A と同じく `p3_run_koha9face.py` の再実行だけで反映される
（B の上流手順・アンカー再マップは不要）。髭は口元ごと平行移動して形を保つ。
胸の凹ませで「口が長く見える」との指摘（2026-07-16）への対応で追加。

同じく後段の `p5_face_weight_fix_blender.py`（4.965）はアニメ時の顔の潰れ・
左右非対称・胸の膨らみ対策（顔を neck1/head 均一混合に統一、前胸の head 支配を
首根・胴へ移譲）。数値は同ファイル冒頭の定数（FACE_*/CHEST_*）。

### B の手順（順番厳守）

```powershell
# 1. 変形前の npz を保存（アンカー再マップの OLD になる）
Copy-Item output_v2\textures\p3_uv_position.npz output_v2\backup_p4face2_20260715\p3_uv_position_stage2.npz

# 2. 上流チェーン再生成（再バインド→尻尾カール→リターゲット→ゲート）
python pipeline_v2\p2_run.py

# 3. UV→3D ベイクを単体で再生成（p3_run.py --bake は使わない = p3_koha/koha9 を上書きしない）
python pipeline_v2\p3_uv_bake.py --glb output_v2\base\p2_koha.glb

# 4. アンカーの移動量を確認
python pipeline_v2\p4_remap_anchors.py

# 5. face パイプライン（ゲート+キャプチャ込み）
python pipeline_v2\p3_run_koha9face.py
```

手順4の読み方:
- 各行の `d=(dx,dy,dz)` が変形によるアンカーの移動量。**±0.002 (2mm) を超えた行があったら**、
  その新座標 `new=(...)` を `p3_paint_texture_koha9face.py` の `EYES`/`FACE_BLOBS`/`NOSE` と
  `p3c_add_whiskers_blender.py` の `ROWS`/`BROWS` に書き写してから手順5へ
- **意図しない場所のアンカーが動いていたら変形のバグ**（例: 耳しか触っていないのに頭頂が動く）。
  手順5に進まず変形を見直す
- `p4_remap_anchors.py` の `ANCHORS` 表は「現在のスクリプトの値」と同期させておくこと
  （書き写したら ANCHORS も同じ値に更新する）

### メッシュ調整の注意（過去に踏んだ罠）

- **耳の拡幅・収束は EAR_BASE_Z(0.252) より上（=板）だけに掛ける。** 付け根の「なじませ帯」に
  掛けると頭皮の中央が横に歪む（実測 ~10mm）。なじませ帯に掛けてよいのは一様な平行移動だけ
- `BROW_H` の下端を目の高さ（0.209）まで下げると目の絵の投影が歪む
- `EAR_UV_OFFSET` は p1a と塗りスクリプトで**必ず同じ値**（現状触る理由なし）
- 変形は `shape_head`（`fit_bones` の後）にあること。前に置くと全身の骨フィットが狂う

---

## 失敗したときの戻し方

- 変形前の一式は `output_v2/backup_p4face2_20260715/` にある
  （p2_koha.blend/glb・face の glb/blend/テクスチャ・旧 npz）
- パラメータを戻して再実行すれば同じものが再生成される（パイプラインは決定的）
- ゲートが FAIL したら、その GLB は Godot に持ち込まない

## 迷ったら

1回の調整は**1〜2個の数値だけ**変えて、`p3k9f_head_front.png` を
`input/glb確認/顔検討2.png` と並べて見比べるのが速い。
大きく変えるより ±10〜20% 刻みで。
